{
  "name": "ws_filewatcher",
  "version": "1.0.0",
  "description": "A backend package that watches files. When file changes, it sends the changed file via ws to the frontend.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Xiao Gui",
  "license": "MIT",
  "dependencies": {
    "ws": "^3.2.0"
  }
}
